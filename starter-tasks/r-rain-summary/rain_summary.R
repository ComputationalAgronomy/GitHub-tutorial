count_rainy_days <- function(rain_values, threshold) {
  rainy_days <- 0

  for (value in rain_values) {
    # TODO: complete condition and increment
    if (value > threshold) {
      # increment here
    }
  }

  return(rainy_days)
}
